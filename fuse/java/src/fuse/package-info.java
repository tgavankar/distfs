/** Java portion of the FUSE driver for Linux and MacOS X.

    <p>
    The lone class in this package, <code>Fuse</code>, is declared as
    package-private. Notionally, the native code that drives this class is
    considered to be in the <code>fuse</code> package, and other code should not
    be able to access the <code>Fuse</code> class.
 */
package fuse;
